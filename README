Required GLFW dependencies for Wayland and X11:
(https://www.glfw.org/docs/latest/compile_guide.html#compile_deps_wayland)

sudo apt install libwayland-dev libxkbcommon-dev xorg-dev

BUILD:
./build.sh

RUN:
cd build
./ft_vox [SEED]